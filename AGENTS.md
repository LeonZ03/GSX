# GSX250R 实车重建 — AI 工作记录

## 项目要求
- 主依据为 IMG/ 的 10 张用户实车照：蓝色漆、白色大幅 SUZUKI 版画、荧光黄轮圈贴。不得改用网上其他配色。
- 还原黑色左右对称管状护杠、左把手机支架、方向阻尼器、黄色双行京 B 车牌。排除尾包、网绳、手套和骑手。
- 用户明确要求使用本机版本；实际为 Steam Blender 5.2.1 LTS，不再使用早期临时下载的4.5便携版。
- +X 右、+Y 前、+Z 上；米制场景，脚本参数按 mm / 1000。轴距1430 mm；前110/80-17、后140/70-17。
- 交付十阶段 .blend、FBX/GLB/OBJ、六视角4K Cycles、脚本、参考归档、README及开发记录。
- 不得宣称未经外观验证的100%还原、扫描精度或产品摄影级验收。

## 参考判断
- 57、59 为用户选定的京 B 牌；60 室内照片是另一旧牌。真实字符只在私有配置读取，禁止在文档或日志中重复。
- 62/63 显示三角管护杠；61/66 是较早单滑块状态。采用三角管护杠并镜像另一侧。
- 65 是驾驶位手机支架与横向阻尼器依据；64 是左侧链传动依据。
- 已归档官方108页零件目录、8页型录、八张官方转台图；来源索引 references/sources.md。
- 没有标定正投影、底面及隐藏安装点。照片参考摆放不等于完成镜头标定，未知尺寸必须标注估计。

## 本地隐私与仓库
- 真实牌号仅 config/*.local.json、忽略的本地 blend/导出/渲染。不得推送用户照片、真实车牌或派生预览图。
- IMG、blends、exports、renders、qa、references/local、references/public 等均被 Git 忽略。只提交代码、需求与来源索引。
- 缺少私有配置时使用 LOCAL / GSX250。render.py --preview 会在临时渲染场景替换实际牌号，不覆盖源文件。
- 自动审批曾拒绝回传含真实牌号的后视图、发布派生预览图片。已分别采用通用牌号检查图和仅推送文本代码完成可行部分，不绕过限制。
- 用户仓库 origin 为 LeonZ03/GSX。Git命令使用命令级 safe.directory，不修改用户全局配置。

## 当前实现与可重建性
- 主构建入口 scripts/build_all.py，顺序调用基础构建、refine、polish、finish、fix_details、surface_refinement，再准备成片设置和验证源文件。
- r6.2 修复早期车头上罩与侧罩间的大空隙：指定外边界共享顶点位置；左右边界检查误差0 mm。
- 重建盾形灯罩与内凹分面反射碗，减弱风挡反射；重新投射大字贴花，并加密跨曲面三角形。
- 最新尺寸：长2074.2、宽736.4、高1102.0 mm；轴距锚点1430 mm。护杠主要控制点镜像误差0 mm；25 mm管径是照片估计。
- r6.2 有895个导出网格，365,711顶点、411,321面。导出计数以最新 qa/export_report.json 为准。
- 最终源文件只有一个场景，没有参考空物体、比例块和丢失外部图像。HDRI与字体打包。
- 本任务已通过Blender MCP调用本地脚本；长时间渲染/导出使用同一本机Blender后台进程。
- 工具的普通沙箱执行出现 helper_unknown_error；本地项目文件操作通过正常用户审批执行，范围限本工作目录。
- 唯一保留的本任务 MCP 服务监听127.0.0.1:9876；源场景载入10_final.blend。

## 检查约定
- 修改几何后必须更新导出和所有受影响最终图，不能用旧图代表新模型。
- validate_source.py 检查求值几何包络、指定曲面边界、护杠镜像、车牌配置一致性和外部资源。
- validate_exports.py 分别回读GLB/FBX/OBJ；audit_artifacts.py生成本地哈希清单。
- 通过GSX_ROOT把全量重建测试隔离到 qa/rebuild_public，禁止测试覆盖车主版。脚本自身仍从项目 scripts 读取。
- 十阶段快照是制作过程记录；09与10包含后续精修。早期快照不代表最终车身。
- 单独重复运行早期修正脚本可能累积缩放；需要重建时使用 build_all.py。

## 当前文件状态与精修待办
- r6.2 六张3840×2160、160 samples的Cycles成片全部输出；导出副本已按部件整理UV并完成三格式回读，最新无私有输入全量重建通过。
- README、docs/validation.md、qa/delivery.json与21个交付文件的哈希清单已更新。Git仓库仅保存文本代码，提交状态用git status核对。
- 外观仍存在近似：车头具体曲率、整流罩小接缝、字形、发动机铸件、灯内结构、轮胎品牌纹路与使用痕迹。
- 后续精度工作优先：照片镜头拟合、逐笔矢量贴花、机械外壳细化、隐藏支点证据、统一烘焙图集与穿插检查。不能把文件验证通过等同外观100%。

## 委派记录
- 仅公开资料整理任务 reference_research；请求配置 gpt-5.6-luna / low，禁止下级委派。运行环境未回报实际配置，不能声称已确认运行模型。
- 主智能体负责照片判断、Blender造型、集成、视觉检查、导出与验收。
