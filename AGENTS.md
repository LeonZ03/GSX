# GSX250R 实车重建 — AI 工作记录

## 不可变需求
- 来源为 IMG/ 的 10 张用户实车照，车身金属蓝、白色大幅 SUZUKI 版画、荧光黄轮圈贴；不得换成其他配色。
- 还原黑色管状左右对称护杠、左把手机支架、方向阻尼器、黄色双行牌照。尾包、网绳、手套和骑手不建模。
- 真实牌号仅允许存在于 config/*.local.json 和忽略的本地产物。不得提交用户照片、真实车牌或含牌号的 blend/贴图/渲染/导出。
- +X 车辆右、+Y 前、+Z 上；单位米，参数按 mm / 1000。轴距 1430 mm；前110/80-17、后140/70-17。
- 交付十阶段 .blend、FBX/GLB/OBJ、Cycles 4K、脚本、参考归档、README。不宣称未经验证的 100% 或计量级精度。

## 参考与歧义
- 57、59 两张室外后视照片牌号一致，60 室内旧照片为另一牌；默认采用前者，本地记录。
- 62/63 清晰显示三角管式护杠；61/66 是较早单滑块状态。统一采用管式护杠。
- 65 是驾驶位手机支架与横向方向阻尼器的主要证据；64 是左侧链传动。
- 缺少标定正投影、底面及覆盖件内部，不能把透视照片当作工程蓝图。

## 环境与进展
- 2026-09-18：看完所有 10 张照片；已连通本机 Blender MCP（初始安装5.2.1）。正在准备4.5便携版。
- 已创建私有素材排除规则、公开来源索引。脚本与版本库正在搭建。
- 公开资料整理子智能体：gpt-5.6-luna / low，显式指定，禁止下级委派。

## 验收计划
1. 创建参考场景与按轴距标定的近侧视平面；保存对齐报告。
2. 参数化生成轮组、叉架、机械、覆盖件、附件与自制版画；各阶段独立保存。
3. 正侧/前后3/4渲染对照照片，调整外形。
4. 检查尺寸、护杠对称、UV、外部资源、导出再导入；保存 qa/ 报告。
5. 更新 README 与本记录后提交脚本到用户指定仓库。

## 开发约定
- 建模主脚本 scripts/build_motorcycle.py；仅在专门生成的 GSX 场景里修改。
- 通过 Blender MCP 调用脚本。长时间渲染允许使用项目便携 Blender 后台进程。
- 生成内容与原始照片分离；保持可重复构建，不修改 IMG/。
- 任务复杂且有独立工作才委派，机械工作选择经济模型；主智能体统筹验收。

## 最新状态（r4）
- 用户追加要求已落实：以本机 Blender 5.2.1 LTS 为准；车牌明确选京 B；缺角主动搜索，始终以实车版画/附件为主。
- 已经通过 Blender MCP 建立完整分件首版与四轮外观调整，保存十阶段源文件。
- 导出已完成：907 网格部件，约 120 万顶点/202 万面；GLB/FBX/OBJ 均有 UV 与材质。GLB 回导通过，另两格式回导进行中。
- 实际几何包络：长2074.2、宽736.4、高1102.0 mm；轴距锚点1430 mm。主要护杠控制点镜像误差0 mm。
- 6视角4K Cycles渲染进行中；十阶段需要的检查视角正在补齐。
- 尚非100%还原：车头/灯具/整流罩曲率、字形和机械铸件近似；未逐件验证无穿插。README 已明确禁止夸大完成度。
- 隐私：真实牌号仅 config/user.local.json；IMG、blends、exports、renders、references/local、qa 均在 .gitignore。自动审批曾拒绝把含真实车牌的后视图回传会话，已改用通用占位牌号的检查图继续验收，不绕过该限制。
- 环境：普通文件工具出现 helper_unknown_error；采用批准的正常用户本地执行，所有写入限定本项目。MCP服务为本任务创建的后台进程，不修改其他项目。
- 关键脚本：build_all.py 顺序调用 build_motorcycle/refine_motorcycle/polish_motorcycle/finish_motorcycle；render.py；render_stages.py；export_model.py；validate_exports.py。
- 修改后需要重新出图/导出，禁止使用旧图代表新模型。单独修正脚本存在累积操作，应从初始构建按顺序运行。

## r5.1 验收修正
- 修正贴花：文字 bevel/extrusion 重叠投影导致共面闪烁，已重建为单层几何；主模型现 378,093 顶点/431,000 面，保留907部件。
- 补充自制 Suzuki S 形徽标与右侧ヨシムラ文字近似；风挡/灯罩为直透与反射混合，GLB转为Principled透射近似。
- 调整整体相机留白，HDRI仅参与照明，摄像机背景使用干净颜色。
- 三格式更新后再次回导通过。真实车牌本地比对为真；最终源场景数1，参考物体0，比例块0，缺图0。
- 阶段检查图已经全部完成。最新4K六视角正在重新渲染；不要使用r4旧图或旧面数替代r5.1结果。
- 官方8页型录通过断点续传补齐并验证；此前“型录下载损坏”的问题已解决。

## 可重建性检查
- 全量 factory-startup 构建在 qa/rebuild_public/ 独立目录通过；未复制原照片或私有配置。
- 修复 prepare_final.py 的 GSX_ROOT 隔离目录支持，避免测试写回原项目。缺少私有配置时正确使用 LOCAL / GSX250。
- 添加 scripts/gsx.ps1，统一 Build/Preview/Export/Validate/StageChecks/Render/All 入口；语法检查通过。
- 代码与文档已推送用户指定 origin/main；自动审批拒绝发布派生预览图片后，改为只推送不含实际牌号的文本与代码，推送成功。模型/图片仍只在本地。

## 本轮文件交付状态
- r5.1 六张最终图全部完成，3840×2160、Cycles、160 samples；总渲染耗时见 qa/delivery.json。阶段检查图12张已完成，另有5张交互修正检查图。
- 10 个阶段 blend、5 个导出相关文件（含MTL和纯网格blend）及6张最终PNG，共21个文件纳入 SHA-256 清单。
- 最终校验通过：文件存在/PNG分辨率、三格式回导、无缺失材质或UV、无私有配置的全量重建、脚本语法检查。
- 整车仍是可编辑重建首版，尚未100%匹配实车；继续工作时按README列出的车头/整流罩曲率、贴花描绘、机械细节、UV与穿插检查推进，不能把文件交付通过误解为视觉还原完成。
- 唯一保留的本任务 Blender MCP 监听进程在127.0.0.1:9876，已载入本地10_final.blend；多余非监听进程已关闭。
