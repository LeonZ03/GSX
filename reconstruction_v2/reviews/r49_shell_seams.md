# r49 外板接缝与前罩复核

## 当前 r49：外板接缝与车头侧面重建

唯一工作模型：`reconstruction_v2/model_history/GSX250R.blend`。本机 Blender 5.2.2 LTS，经 Blender MCP CLI 修改、检查和保存；本轮未启用子智能体，未使用 computer use。

- **油箱与坐垫两侧外板：**原侧板缺少向内折回的上沿，俯视能透过大缝看见车架。现在外壁、上沿折返与贴合边由连续四边面控制；边界参照当前坐垫、油箱与侧饰板的求值表面重新布置，并分别处理尾罩和侧饰板接缝。保留正常装配分件，接缝尺寸是建模推定，不是实测公差。
- **车头侧面：**替换旧侧罩与新版前脸之间不一致的接边，重新生成侧回折、上侧罩与驾驶位内板的对应边界；下部侧翼改为向前下方收尖，收窄下部宽度，修正后缩、鼓起的侧脸。邻接前包围同步留出装配缝，清理曲面阶梯和切削碎片。灯罩、油箱与座垫自身控制仍保持上版。
- **核对：**重看IMG中的65驾驶位、69左前侧和72前脸实拍；固定69照片镜头下输出改前改后灰模。同时新增固定俯视、侧缝和车头侧视检查相机供模型比较，未用新检查相机替换照片标定。69属于历史无护杠状态，不能用它判断现款护杠显隐。

12个作用域对象/相邻件均有有效网格，所查开放边、非流形边、零面积面为0；4个重点表面无未解释自交。侧包围保留1个被判定为边界接触的原始候选计数。10组相邻表面交叉为0。前罩是1个连通实体，座侧板、驾驶位内板与左右侧包围分别保留2个完整侧件；没有断离切削碎片。612个受保护对象/相机的源几何与变换签名不变。检查仅针对本轮作用域，不等于整车装配或实车形似通过。

**B/C仍为NOT_PASSED，未宣称1:1。** 整车轮廓IoU、正式关键点误差及两个独立角度仍未通过；前脸整体比例、灯具内部与隐藏安装尺寸仍有偏差。1 mm邻面避让和0.85 mm体素整理是构造设置，不是实车精度。本轮不进入最终贴花、材质、4K和成品导出。

本地模型提交：`a8063151e64965825dc72758676c5c898b6eb86c`，已回读验证SHA256一致。r48仍可由`a41c4dbcc40ff288b903ce9f4b44a7bf08d9a42d`恢复。继续只维护一个工作blend；照片、源模型、号牌、渲染、标注与QA不推送。

检查图：`reconstruction_v2/renders/r49_seams_before_after.jpg`与`r49_front_before_after.jpg`；局部图在`renders/assembly_review/r49_{TankSeatTop,SideSeam,NoseSide,Nose}.png`。固定照片69的灰模位于`renders/gray_r48_69.png`和`renders/candidate_r49/gray_r49_69.png`；后者是通过集成前的最终候选渲染，未把它冒充保存后重新渲染。保存后已回读并生成上述4张局部图。局部检查为`qa/shell_r49.json`与`qa/r49_integration.json`。

重建入口`integrate_r49.run()`仅接受已提交r48，拒绝重复运行于r49；父控制快照在`data/revisions/r48_shell_baseline/controls.json`。新Body_SeatSide为21×11控制网格，Body_UpperSideCowl为6×13；其余现行曲面源见manifest。保留`Tool_R49NoseJoint`、`Tool_R49TrimJoint`、`Tool_R49TailJoint`的实时邻面依赖；隐藏前罩控制面不能删除。侧包围接缝采用EXACT布尔，不能换回产生两个微小自交的MANIFOLD候选；上折返与驾驶位切口保留体积整理与碎片过滤，仍需检查左右各一个连通侧件。不要在精修源盲目执行旧apply_cages或历史入口。完整独立重放未认证。

## 被排除的候选

第一候选旧EXACT座垫避让产生整片空网格，已拒绝；网格检查必须要求顶点/面非空，不能把空网格的零错误当作通过。后续候选边界切削存在碎片和自交，未保存为主模型。最终清理后检查前罩一个主体、其他三组各两个侧件，防止通过删除主要表面取得零计数。最终侧包围1个边界接触保留在原始报告中。
