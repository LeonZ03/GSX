# r10 骑手座与连接修订

状态：**灰模候选，M1_NOT_PASSED，仍未达到1:1**。

## 本轮范围

只修改 Seat_Rider、Body_SeatSide 的两处上沿和 Body_PillionBase 前端。相机、后座本体、尾罩主体及其它控制网格未改；r09及旧源保留。新增隐藏的 Tool_SeatClearance 为构造辅助体，仍保留19个四边面控制网格，共588对象。

骑手座使用13个毫米参数控制长度、宽度、上沿和底面，幅度受限；由实际细分网格导出线性映射并用62/69固定候选相机拟合。3次短迭代，无相机优化。7个参数触及预设边界，说明证据/参数化仍有局限，不扩大边界强行贴图。

63补充了可见近侧下缘的开放标注，未用于本轮优化；它在之前已用于项目，不能当作整个重建的独立holdout。上部尾包遮挡和不清楚的前端接口未补造闭合轮廓。

## 局部诊断

| 骑手座视角 | 中位距离 r09 → r10 | P95 r09 → r10 | 指标范围 |
|---|---|---|---|
| 62 | 7.2 → 3.0 px | 16.6 → 13.0 px | 闭合人工描边与分件投影，双向距离 |
| 69 | 12.5 → 6.0 px | 22.6 → 16.8 px | 同上；69镜头仍为候选 |
| 63 | 12.1 → 9.0 px | 21.8 → 18.6 px | 开放下缘到模型的单向距离；未参与本轮优化 |

人工描边约±4 px；这些是轮廓最近距离，不是同名特征点误差、完整可见像素分割或整车还原率。62/69局部IoU约0.750/0.775，不满足98%目标。单独部件投影不处理全部相邻部件遮挡，仍需结合实际渲染检查。

## 接缝与文件检查

- 骑手座两端补为四边面封口；求值网格无非流形边。
- 根据座垫底面射线检查，收进座下侧板两段上沿，降低过渡壳前端。
- 控制点修正本身仍有穿插；增加可撤销布尔间隙，辅助体共享骑手座控制网格并跟随其变换。3 mm间隙是建模构造值，不是实车测量。
- 求值表面检查中，骑手座与尾罩、座下侧板、后座过渡壳的相交对均为0；这只通过该局部检查。油箱连接仍检出相交，不能称完整装配通过。
- 19个控制网格仍全四边面，镜像/细分保留；轴距1430.0001 mm、盘径290/240 mm名义构造检查维持。车牌仍为空白灰模。
- Tool_SeatClearance 隐藏且标记 export_exclude。最终导出时应在应用必要修改器后排除辅助体；现阶段不导出成品。后续若显式替换骑手座mesh数据块，必须同步辅助体引用。

## 仍未通过 / 下一步

后座r09约90 mm高度修正仍未获独立验证，本轮未进一步改动。候选相机的主点、畸变、轮圈尺寸假设未消除；两个可靠独立角度仍缺。下一步集中骑手座前端—油箱连接、座尾接缝及固定参考关键点，先解决连接和标定不确定性，不进入贴花、材质或4K。

本地源：blends/10_gray_review.blend。对照：renders/seat_tail_r10_comparison.jpg、review_r10_contact_sheet.jpg。误差：qa/seat_tail_r10.json。接缝：qa/seat_interfaces_r10.json。构造：qa/geometry_r10.json。原照、标注、镜头、含照图、blend及真实牌照均不推送。

本轮只读复核子任务请求为gpt-5.6-luna/low；工具未回传实际配置，无法确认实际运行配置。主智能体负责标注、建模、三视角复核和交付。
