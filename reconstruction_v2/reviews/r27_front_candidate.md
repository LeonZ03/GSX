# r27 车头—侧罩候选收尾

本轮按用户“收尾汇报”的新指令停止扩展建模。**阶段 B、C 都未通过，r27 未提升为正式工作基线。**

## 实际完成

- 重新核对右侧、右前、左前三张照片，纠正外侧蓝色尖角与灯罩旁内侧点混淆的对应关系。相机、固定排除区未修改；新开放边界标注仅本地保存。
- 侧罩前上沿重建，增加峰值附近控制行；保留后段三行与下沿主要控制。同步制作上罩、侧面折返与驾驶位内衬候选，共4组可编辑四边面控制。
- 连续检查三版候选，回退导致更多重叠的前罩侧回折面修改；最终来源直接继承r26原回折面。
- 四个固定角度完成灰模与叠加检查。r26与旧源均保留。

## 局部边界诊断

以下为实际求值网格前上沿的开放曲线双向距离；三张图都参与调整，手工边界约±5px。不是闭合部件轮廓、关键点误差、独立复核或整车相似度。起点是约Y=300mm的对应区间。

|照片|中位数 r26→r27|P95 r26→r27|
|---|---:|---:|
|62|7.3→0.6 px|27.8→3.1 px|
|63|15.5→4.3 px|62.7→6.8 px|
|69|11.9→1.1 px|60.4→3.6 px|

这些结果支持前上沿对应关系改善，不支持整个车头通过。不能把小于标注不确定性的数值解释为亚像素真实精度。

## 未通过与保留策略

- 侧罩、上罩、驾驶位内衬所查非流形边、退化面、自交候选为0；侧面折返仍有13个非相邻三角接触/自交候选，未判通过。
- 侧罩—散热器仍有202个表面相交候选；前罩总成—上罩1945个；上罩—侧面折返209个。此计数是排错定位，不能跨拓扑当作质量分数。
- 车头—侧罩需要重新组织共享边界与厚度，不应继续用多个相交薄片修补。最新图中形态差距仍明显。
- C阶段本轮只做了排气/发动机证据复核，没有新增机械建模交付。现有铸件仍过于通用；排气隔热罩仍需从平板改为有包裹截面的独立罩体。不能描述为B/C完成。
- 926对象；相机和机械对象保持r26。4组候选控制单独归档，**data/control_cages仍是r26，不要用它覆盖r27候选**。

## 文件与复现

- 工作基线：`blends/26_gray_review.blend`。
- 待验收候选：`blends/27_front_review_candidate.blend`。
- 三角度前后对照：`renders/stage_bc_r27_comparison.jpg`。
- 四角度检查：`renders/review_r27_{62,63,64,69}.jpg`及`review_r27_contact_sheet.jpg`。
- 候选控制：`data/revisions/r27_front_candidate/`；旧控制：`data/archive/r26_front/`。
- 在Blender MCP后台打开独立r26后调用`apply_front_r27_candidate.apply(scene, fresh_output_path)`。拒绝已有输出。候选声明UNACCEPTED_ASSEMBLY_OVERLAPS，不会写canonical目录。
- 本地`qa/front_r27/geometry.json`、`upper_rail_metrics.json`、`replay_final.json`记录诊断与复现比较。最终从r26独立重放，926对象的源几何、变换及相机签名一致；不包含材质/修改器全量等价性认证。早期`27_gray_review.blend`从JSON恢复回折面时存在面连接顺序/方向差异，不能仅按浮点误差处理。最终源直接继承r26该面，四角度渲染和几何QA均已改用最终候选重新生成；早期比较保留为失败诊断。

## 后续优先级

1. 将车头外壳、侧肩和下侧罩按真实分件建立连续控制边界，解决重叠后再提升候选；保持镜头和已有边界固定。
2. 重建排气筒异形截面、包裹式隔热罩、后盖/出口及连接；再做发动机铸件外轮廓。
3. 补全主要部件闭合可见区域与有效独立角度，才能评估B/C阶段门槛；D/E继续不启动。

没有可信的剩余轮数估计；先前预算不能当作已完成承诺。本轮耗时主要在对应关系纠错和候选验证，产出是局部边界改善和明确失败项，未达到用户要求的整组B/C完成。

## 委派

启动front_evidence、mechanical_evidence两个子任务，均请求gpt-5.6-luna/medium；工具未确认实际配置。前者提供只读形态提示；后者本机读图链路失败，未将未经观看的照片推测当作验收，主线程另行核对了排气照片。所有建模、回退和验收由主线程完成。没有Computer Use。
