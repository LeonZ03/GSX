# r13：油箱后缘与座垫接口小幅修正

状态：**M1_NOT_PASSED，外形改善有限，仍未达到1:1。** 最新独立源为`blends/13_gray_review.blend`。

## 范围

本轮只改 Body_Tank 后部5段控制截面的Y坐标，依次后移3.5、5、10、10、3.5 mm；前部截面、X/Z坐标不动。保留19个四边面控制网格、589对象及原修改器。座垫、后座、饰板、相机未改。没有保留新增倒角修改器，也没有修改材料。

相机和既有标注随r11冻结。35份原有输入中仅Body_Tank控制JSON变化；r11源文件未变。实际场景相机矩阵/内参签名相同，五个被检查的相邻分件求值网格完全相同。

## 新增依据与候选筛选

为62、63、69三个固定视角标注油箱后缘、油箱与座垫接缝的可见开放边界，并在62/63记录黑饰板上沿。人工边界约±4 px；没有补造隐藏线，也没有称其为完整分割或同名三维关键点。62/63用于候选排序，69用于额外交叉检查，不能称为独立验收视角。

先检查18组较大参数候选，再检查12组较小候选，另有未改基线。后移、收窄和抬高由平滑截面权重控制，未逐点拉扯网格。较大后移能明显改善62，但使69后缘变差，未采用。小候选中2组出现非相邻自交候选，也未采用。

选中`e10_w0_z0`：只保留小幅后移。另试的1 mm切口倒角出现15组自交候选，已否决并保存失败源，仅用于诊断；它未进入最终模型。**接口尖折尚未完全消除。**

## 从实际渲染复核

使用同一Blender 5.2.2 LTS、同一镜头和原分辨率进行改前/改后局部渲染。参数筛查使用带深度的分件投影；补入23个其它可见物件的局部遮挡，并用Cycles整帧分件颜色输出核对。

实现校验中，局部分件遮罩与Cycles一致率约99.89%–100%；**这只是计算实现与渲染的一致性，不是照片相似度，不能用于98%外观门槛。** 最终照片距离直接从Cycles整帧分件输出计算。

另核实了Blender裁切取整：62实际原点为(540,720)，63为(445,761)，69为(930,525)。通过裁切图与整帧图逐像素对应确定，而非凭边界浮点数猜测；原照裁切与模型原生像素对齐。

| 视角 | 后缘平均距离 r11→r13 | 后缘P95 r11→r13 | 接缝平均距离 r11→r13 |
|---|---|---|---|
| 62 | 25.1→21.2 px | 26.9→22.8 px | 9.1→8.5 px |
| 63 | 7.3→3.8 px | 12.4→7.8 px | 4.6→4.1 px |
| 69 | 2.8→2.8 px | 7.8→4.4 px | 3.5→2.9 px |

69后缘中位距离从1.5升至2.9 px，平均值基本不变、P95下降；不能宣称所有指标都改善。接缝变化多小于人工描边不确定性，不作为明确收敛证明。黑饰板上沿诊断保持不变，仍有局部偏差。

上述指标是一侧开放参考线到可见分件边缘的最近距离。接缝筛选使用距可见座垫10 px以内的油箱边缘，这是图像选择规则，不是实车间隙。它们不是完整轮廓IoU、同名关键点误差或整车完成百分比。

## 几何检查和限制

最终源的油箱—座垫三角相交对为0；两者非流形边和非相邻自交候选均为0。骑手座与尾罩、座下侧板、后座过渡壳仍无局部表面相交。轴距1430.0001 mm和前后盘290/240 mm的名义构造检查维持；没有实车毫米精度证明。

62后缘仍有约21 px平均偏差，油箱后半部曲率、可见折线与接缝尖折未通过。大参数调整在多视角间产生冲突，不能继续整体后移来迎合62。下一步补跨图稳定特征并分开处理后部曲率与接口，必要的标定修订独立记录；不能同时调相机和曲面。69镜头和后座高度的不确定性仍沿用r12结论。暂不进入贴花、材料或4K。

## 文件与复现边界

- 源：`blends/13_gray_review.blend`。
- 局部对照：`renders/tank_interface_r13_comparison.jpg`。
- 四角度：`renders/review_r13_{62,63,64,69}.jpg`、`review_r13_contact_sheet.jpg`。
- QA：`interface_review_r13.json`、`interface_raster_validation_r13.json`、`interface_frozen_r13.json`、`tank_interface_13_gray_review.json`、`geometry_r13.json`。
- 失败倒角源：`blends/13_interface_candidate.blend`，**不可当作最终版**。未倒角候选为`13_interface_unrounded_candidate.blend`。
- `generate_interface_candidates.py`仅从r11生成本地试验，拒绝已有输出；`--small`为第二批。不得对当前精修JSON直接重跑历史候选。
- `export_interface_meshes.py`读取实际网格和局部遮挡；`render_interface_review.py`支持正常局部图、`--ids`及`--ids-full`诊断；颜色输出不写回模型。
- `validate_interface_raster.py`核对实际渲染和裁切原点，`review_interface_r13.py`生成最终局部图和距离记录。

真实照片、标注、镜头、模型、渲染和相关QA均仅本地；公开提交代码、文档和派生油箱控制网格。

只读interface_evidence子任务请求gpt-5.6-luna/low；工具未回传实际运行配置。子任务图像helper不可用，提供的是文档/代码复核，不能当成图像验收；主线程完成照片查看、标注、改形和实际渲染验证。
