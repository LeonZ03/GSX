# r15：双上缘约束与完整油箱连续修正

**M1_NOT_PASSED，仍未达到1:1。** 最新源`blends/15_gray_review.blend`。本轮从r13连续比较并保留r14，再修正约束遗漏和前端控制范围，推进至r15；中间没有把一次小改当作交付结束。全部建模使用Blender MCP后台，不使用Computer Use。

## r14之后的纠正

子任务复核指出初版`fit_tank_crown.py`的crown项位于视角循环之外，实际只加入最后一张69的上缘。62后缘仍参与，62上缘只做了事后测量；r14实际渲染指标有效，但不能说当时两条上缘都参与求解。主线程确认这是实质遗漏，将约束移入循环并加入实际参与视角断言，随后重新拟合，没有只修代码就停止。

九截面候选的前端仍受固定末截面限制；继续扩展到全部10个截面、40个有界平滑控制参数，完整包含两张上缘及三张后缘。比较权重2和3的最终方案，后者使62后缘和上缘P95变差，未选。选中`full_joint_w2_r15`。

## 改动范围与实际检查

- Body_Tank保持r14的80顶点/69四边面、封闭前后端，全部控制截面可编辑。无旧Solidify，无新增Bevel；镜像、细分、三角化和座垫避让差集保留。
- 相对本轮起点r13，最大控制点位移49.13mm；X/Y/Z最大绝对改变量分别21.03/38.74/29.64mm。这是建模修正量，不是实车尺寸测量。
- 油箱盖XY未变，两物件按新顶面统一抬高5.44mm；盖本身位置/造型仍待实车复核。
- 实际对象签名只有Body_Tank、FuelCap、FuelCap_Core变化；全部相机矩阵/内参、其它对象控制几何/变换未变。原r13/r14独立源保留。
- 油箱、座垫非流形边和非相邻面自交候选均0，油箱与座垫表面相交0；其它三处座尾局部相交保持0。589对象、19个可编辑quad控制网格。
- 封闭外形不含真实油箱壁厚；3mm辅助避让为既有构造余量，不能当作实测均匀缝隙。全车装配尚未通过。

## 实际渲染结果

与r13使用同一Blender5.2.2、固定相机、相同本地照片与原生分辨率；通过裁切/整帧ID图对齐。指标取最终源实际Cycles分件输出，不取求解器预测。62/63/69均参与拟合，64仅作欠约束镜头下的视觉回看，不能称独立验证。

| 照片/边界 | 平均距离 r13→r15 | P95 r13→r15 |
|---|---|---|
| 62上缘 | 13.62→4.31 px | 20.35→17.63 px |
| 69上缘 | 17.05→0.71 px | 29.03→1.43 px |
| 62后缘 | 21.17→16.71 px | 22.81→20.71 px |
| 63后缘 | 3.82→1.49 px | 7.79→2.56 px |
| 69后缘 | 2.82→3.15 px | 4.39→6.19 px |
| 62接缝 | 8.46→6.96 px | 19.27→16.27 px |
| 63接缝 | 4.09→4.14 px | 6.96→6.10 px |
| 69接缝 | 2.86→4.33 px | 6.45→8.63 px |

两条上缘改善明确；**69后缘、接缝有小幅退步，63接缝均值基本不变**。旧标注约±4px，新上缘约±5px，不能将小变化当作确定收敛。62后缘和前部局部曲率仍未解决；肩部折线与实车也不完全一致。

以上是开放参考边界到可见分件边缘的单向距离，接缝筛选为图像空间10px邻近规则，不是实体间隙。没有计算整车还原率、完整分件IoU或同名三维关键点精度，没有通过两个独立角度。

## 文件、复现与下一步

- 源：`blends/15_gray_review.blend`。
- 本地对照：`renders/tank_crown_r15_comparison.jpg`、`review_r15_{62,63,64,69}.jpg`与contact_sheet。
- QA：`tank_review_r15.json`、`tank_frozen_r15.json`、`tank_interface_15_gray_review.json`、`geometry_r15.json`、`seat_interfaces_r15.json`。
- `extract_tank_full_basis.py`只从明确冻结r13提取封闭曲面线性提案；`fit_tank_full.py`读取该提案与本地标注/旧初值，写独立候选；完整修改器与实际渲染必须另验。
- `apply_tank_candidate.py`执行明确的端盖拓扑迁移，拒绝覆盖源；`render_tank_review.py`和`review_tank_r15.py`复核当前实际源。旧r14初版错误求解不能通过已纠正脚本宣称原样复现，旧源和候选数据保留。
- 继续62后缘/前端局部偏差、肩部折线及接缝。必要的相机修订单独记录，不用调镜头掩盖形体；灰模通过前不进入贴花、材质、4K或三格式导出。

照片、标注、镜头、blend、渲染、QA和真实车牌继续本地；只推代码、文档、派生控制网格。只读tank_evidence子任务请求gpt-5.6-luna/low，工具未确认实际配置；主线程核实其约束遗漏发现，完成修复、重算、实际图检及验收。
